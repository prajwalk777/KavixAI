// Replace with your backend API if needed
async function getAIResponse(msg) {

    // Fake fallback (offline mode)
    if (msg.includes("hello")) return "Hello from Kavix Ultimate 🤖";

    return "I'm evolving... connect me to real AI 🚀";
}